# Copy this file to .env and adjust values as needed. Never commit .env with real secrets.

# Application environment label (e.g., local, test, production). Default: "local".
AIC_ENV=local

# Optional OpenAI API key for live LLM workflows. Leave unset for offline tests.
AIC_OPENAI_API_KEY=
